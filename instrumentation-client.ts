/**
 * Client-side instrumentation (Next.js convention).
 * Runs after the HTML document loads but before React hydration.
 * Kept minimal so Next.js can resolve private-next-instrumentation-client.
 */
// Optional: add client-side analytics or monitoring here later
